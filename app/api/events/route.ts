import {clean,sameOrigin} from "../../integrations/security";
const allowed=new Set(["page_view","cta_click","form_start","form_submit","assistant_open","assistant_query","consent_update"]);
export async function POST(request:Request){if(!sameOrigin(request))return new Response(null,{status:403});const raw=await request.json().catch(()=>null) as Record<string,unknown>|null;const event=clean(raw?.event,60);if(!allowed.has(event))return new Response(null,{status:422});const endpoint=process.env.IMTS_ANALYTICS_ENDPOINT,siteId=process.env.IMTS_ANALYTICS_SITE_ID;if(!endpoint||!siteId)return new Response(null,{status:204});await fetch(endpoint,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({event,siteId,path:clean(raw?.path,300),timestamp:new Date().toISOString(),properties:raw?.properties||{}})}).catch(()=>null);return new Response(null,{status:202})}
